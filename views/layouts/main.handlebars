<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Document</title>
</head>
<body>
    <header>
        <nav class="nav-bar">
            <button class="navBar-btn" id="home-btn">Home</button>
            <a href="/homepage">Home</a>
            <a href="/dashboard">Dashboard</a>
            {{#if isLoggedIn}}
            <button class="navBar-btn" id="login-btn" hidden="hidden">Login</button>
            {{else}}
            <button class="navBar-btn" id="login-btn">Login</button>
            <a href="/login">Login</a>
            {{/if}}
            <button class="navBar-btn logout" id="logout-btn">Logout</button>
        </nav>
        {{!-- {{#if dashboard}}
        <div>Your Dashboard</div>
        {{else}}
        <div>The Hack Blog</div>
        {{/if}} --}}
    </header>
    {{{body}}}
</body>
</html>
<script src="../../js/logout.js"></script>